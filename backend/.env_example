PORT=3001
NODE_ENV=development
DB_HOST=localhost
DB_PORT=5434
DB_USER=postgres
DB_PASSWORD=changeme
DB_NAME=netmap
LOG_LEVEL=info
CORS_ORIGIN=http://localhost:5173

# Docker Compose (opcional — valores padrão já definidos no compose)
# VITE_API_URL=http://localhost:3001/api
